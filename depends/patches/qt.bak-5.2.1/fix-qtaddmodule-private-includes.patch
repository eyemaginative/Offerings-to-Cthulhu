--- a/qtbase/mkspecs/features/qt_functions.prf
+++ b/qtbase/mkspecs/features/qt_functions.prf
@@ -57,6 +57,14 @@ defineTest(qtAddModule) {
     MODULE_LIBS = $$eval(QT.$${1}.libs)
     MODULE_CONFIG = $$eval(QT.$${1}.module_config)
     MODULE_MAJOR_VERSION = $$eval(QT.$${1}.MAJOR_VERSION)
+    MODULE_BASE = $$replace(1, _private$, )
+
+    # Private module dependencies must bring both their private include dirs
+    # and the matching public module include dirs. Modern host builds
+    # otherwise miss headers such as <qglobal.h> or <qshareddata.h>.
+    contains(1, _private$) {
+        MODULE_INCLUDES = $$eval(QT.$${MODULE_BASE}.includes) $$MODULE_INCLUDES
+    }
 
     contains(MODULE_CONFIG, internal_module) {
         using_privates = true
